import React from 'react';

const Settings = () => {
  return (
    <div className="settings">
      <h1>Settings</h1>
      {/* Add user management, alert settings, and other configurations here */}
    </div>
  );
};

export default Settings;