import React from 'react';

const Rules = () => {
  return (
    <div className="rules">
      <h1>Rules</h1>
      {/* Add rule-based prioritization engine and rules UI here */}
    </div>
  );
};

export default Rules;